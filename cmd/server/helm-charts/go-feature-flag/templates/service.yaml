apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-feature-flagy.fullname" . }}
  labels:
    {{- include "go-feature-flag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-feature-flag.selectorLabels" . | nindent 4 }}
